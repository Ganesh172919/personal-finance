# FinWise Client (.env)
# Copy to client/.env and adjust values.

# In dev, Vite proxy uses /api to reach the server.
VITE_API_BASE_URL=/api

# Dev-only: Vite proxy target for /api requests.
VITE_API_PROXY_TARGET=http://localhost:3000
